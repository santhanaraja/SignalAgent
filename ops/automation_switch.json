{
  "kill_switch": "on",
  "_comment": "The ONLY value that lets orders through is \"off\". Anything else — a typo, a missing key, an unreadable file — reads as KILLED. Edit from the GitHub iOS app or any browser; the run re-reads this immediately before every submit."
}
