services:
  - type: web
    name: signalagent-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python ticker_api.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    autoDeploy: true
