__pycache__/
*.pyc
.DS_Store
*.egg-info/
.env
venv/

# Local Netlify folder
.netlify

# Universe source layer — local build caches + redundant local copies.
# public/universe_candidates.json and public/universe_ranking.json are the
# committed artifacts the API serves; data/universe_active.json is the
# committed weekly rotation cache consumed by signal_engine.
data/universe_cache/
data/universe_candidates.json
data/universe_ranking.json

# Build 4 backtest — raw market-data pulls are cached locally, never
# committed; the committed artifacts are the results JSON + report + charts.
data/backtest_cache/
